### Mistake List
- id was not destructured in map()
- React expects a key when rendering a list.
- Rendering strings instead of variables. Curly braces tell JSX to evaluate a JavaScript variable.
- Putting key inside the component
- Parentheses around JSX in map() 
- 