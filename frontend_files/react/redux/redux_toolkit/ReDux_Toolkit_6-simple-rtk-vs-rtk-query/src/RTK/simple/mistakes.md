| ❌ What you did                                                            | ✅ What it should be                                                          | Why                                                                                                                                            |
| ------------------------------------------------------------------------- | ---------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| `.addCase(fetchUser.pending, ...)`                                        | `.addCase(thunkFetchUser.pending, ...)`                                      | Your thunk is named `thunkFetchUser`, not `fetchUser`. The names must match exactly.                                                           |
| `.addCase(fetchUser.fulfilled, ...)`                                      | `.addCase(thunkFetchUser.fulfilled, ...)`                                    | Same issue: using an undefined thunk name.                                                                                                     |
| `.addCase(fetchUser.rejected, ...)`                                       | `.addCase(thunkFetchUser.rejected, ...)`                                     | Again, use the actual thunk name.                                                                                                              |
| `(state, error) => { state.error = action.error }`                        | `(state, action) => { state.error = action.error.message }`                  | The second parameter is always `action`, not `error`. You were using `action` without defining it.                                             |
| `import simpleSlice from "./simpleSlice"` and `user: simpleSlice.reducer` | `import simpleReducer from "./simpleSlice"` and `user: simpleReducer`        | You exported `simpleSlice.reducer` as default, so the imported value is already the reducer function. There is no `.reducer` property anymore. |
| `catch(error) { return "Unable to fetch via simple RTK"; }`               | `catch(error) { return rejectWithValue("Unable to fetch via simple RTK"); }` | Returning a string marks the thunk as **fulfilled**, not **rejected**.                                                                         |
| No import for `thunkFetchUser` inside the slice file                      | `import { thunkFetchUser } from "./thunkFile";`                              | `extraReducers` cannot use the thunk if it isn't imported.                                                                                     |
